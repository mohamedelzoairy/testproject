version: "3"
services:

  maven1:
    build: ./App-1
    ports:
      - "8080:7766"
    container_name: app-1
  
  maven2:
    build: ./App-2
    ports:
       - "8080:8866"
    container_name: app-2

  nginx:
    build: ./reverse-proxy
    ports:
      - "80:80"
    container_name: reverse-proxy
